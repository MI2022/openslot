'use client'

import Link from 'next/link'

export default function Home() {
  return (
    <div className="min-h-screen flex flex-col bg-white text-gray-900">

      {/* Navigation */}
      <header className="sticky top-0 z-50 border-b border-gray-100 bg-white/90 backdrop-blur-sm">
        <div className="mx-auto flex h-16 max-w-6xl items-center justify-between px-6">
          {/* Logo + Name */}
          <div className="flex items-center gap-2">
            <div className="flex h-8 w-8 items-center justify-center rounded-lg bg-indigo-600 text-white">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" className="h-5 w-5">
                <path d="M12 2a10 10 0 1 0 10 10A10 10 0 0 0 12 2zm0 18a8 8 0 1 1 8-8 8 8 0 0 1-8 8zm.5-13H11v6l5.25 3.15.75-1.23-4.5-2.67z" />
              </svg>
            </div>
            <span className="text-xl font-bold tracking-tight text-gray-900">OpenSlot</span>
          </div>

          {/* Nav links */}
          <nav className="hidden items-center gap-8 text-sm font-medium text-gray-600 md:flex">
            <a href="#features" className="hover:text-indigo-600 transition-colors">Features</a>
            <a href="#how-it-works" className="hover:text-indigo-600 transition-colors">How it works</a>
            <a href="#demo" className="hover:text-indigo-600 transition-colors">Demo</a>
          </nav>

          {/* CTA */}
          <div className="flex items-center gap-3">
            <Link href="/login" className="text-sm font-medium text-gray-600 hover:text-indigo-600 transition-colors">
              Log in
            </Link>
            <Link
              href="/login"
              className="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-700 transition-colors"
            >
              Get started
            </Link>
          </div>
        </div>
      </header>

      <main className="flex-1">

        {/* Hero */}
        <section className="bg-gradient-to-b from-indigo-50 to-white py-24 text-center">
          <div className="mx-auto max-w-3xl px-6">
            <span className="mb-4 inline-block rounded-full bg-indigo-100 px-3 py-1 text-xs font-semibold uppercase tracking-widest text-indigo-700">
              Now in early access
            </span>
            <h1 className="mt-4 text-5xl font-extrabold leading-tight tracking-tight text-gray-900 sm:text-6xl">
              Scheduling that{' '}
              <span className="text-indigo-600">gets out of your way</span>
            </h1>
            <p className="mt-6 text-lg leading-relaxed text-gray-600">
              Share a link. Let people pick a time. Done.
              OpenSlot handles availability, bookings, and confirmations so you can focus on the meeting — not the logistics.
            </p>
            <div className="mt-10 flex flex-col items-center justify-center gap-4 sm:flex-row">
              <Link
                href="/login"
                className="w-full rounded-xl bg-indigo-600 px-8 py-3.5 text-base font-semibold text-white shadow-md hover:bg-indigo-700 transition-colors sm:w-auto"
              >
                Start for free
              </Link>
              <a
                href="#demo"
                className="w-full rounded-xl border border-gray-200 bg-white px-8 py-3.5 text-base font-semibold text-gray-700 shadow-sm hover:bg-gray-50 transition-colors sm:w-auto"
              >
                See how it works
              </a>
            </div>
            <p className="mt-4 text-sm text-gray-400">No credit card required. Free forever for individuals.</p>
          </div>
        </section>

        {/* Features */}
        <section id="features" className="py-24">
          <div className="mx-auto max-w-6xl px-6">
            <div className="text-center">
              <h2 className="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">
                Everything you need to manage bookings
              </h2>
              <p className="mt-4 text-lg text-gray-500">
                Built for freelancers, consultants, and small teams who need simple scheduling without the bloat.
              </p>
            </div>

            <div className="mt-16 grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
              {[
                {
                  icon: '🔗',
                  title: 'Shareable booking links',
                  desc: 'Create a meeting type and share a unique link. Anyone can book without signing up.',
                },
                {
                  icon: '🗓️',
                  title: 'Flexible availability',
                  desc: 'Define your weekly availability windows and let OpenSlot generate the open slots automatically.',
                },
                {
                  icon: '⚡',
                  title: 'Instant confirmations',
                  desc: 'Bookers see a confirmation immediately. No back-and-forth, no double bookings.',
                },
                {
                  icon: '🔒',
                  title: 'Conflict prevention',
                  desc: 'Real-time conflict checks ensure no two people can ever book the same slot.',
                },
                {
                  icon: '📋',
                  title: 'Booking dashboard',
                  desc: 'See all your upcoming and past bookings in one clean view. Filter, search, and manage with ease.',
                },
                {
                  icon: '🎛️',
                  title: 'Multiple meeting types',
                  desc: 'Set up a 15-min intro call, a 1-hour workshop, and a 30-min check-in — all with different availability rules.',
                },
              ].map((f) => (
                <div
                  key={f.title}
                  className="rounded-2xl border border-gray-100 bg-white p-8 shadow-sm hover:shadow-md transition-shadow"
                >
                  <div className="mb-4 text-3xl">{f.icon}</div>
                  <h3 className="mb-2 text-lg font-semibold text-gray-900">{f.title}</h3>
                  <p className="text-sm leading-relaxed text-gray-500">{f.desc}</p>
                </div>
              ))}
            </div>
          </div>
        </section>

        {/* How it works */}
        <section id="how-it-works" className="bg-indigo-50 py-24">
          <div className="mx-auto max-w-4xl px-6 text-center">
            <h2 className="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">
              Up and running in minutes
            </h2>
            <p className="mt-4 text-lg text-gray-500">Three steps and you're accepting bookings.</p>

            <div className="mt-16 grid gap-8 sm:grid-cols-3">
              {[
                { step: '01', title: 'Set your availability', desc: 'Tell OpenSlot when you're free each week — by day and time range.' },
                { step: '02', title: 'Create a meeting type', desc: 'Name it, set a duration, and generate your unique booking link instantly.' },
                { step: '03', title: 'Share and relax', desc: 'Send the link. Visitors pick a slot, fill in their details, and you get a booking.' },
              ].map((s) => (
                <div key={s.step} className="flex flex-col items-center text-center">
                  <div className="mb-4 flex h-12 w-12 items-center justify-center rounded-full bg-indigo-600 text-lg font-bold text-white">
                    {s.step}
                  </div>
                  <h3 className="mb-2 text-base font-semibold text-gray-900">{s.title}</h3>
                  <p className="text-sm leading-relaxed text-gray-500">{s.desc}</p>
                </div>
              ))}
            </div>
          </div>
        </section>

        {/* Demo / Mockup */}
        <section id="demo" className="py-24">
          <div className="mx-auto max-w-6xl px-6">
            <div className="text-center">
              <h2 className="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">
                See it in action
              </h2>
              <p className="mt-4 text-lg text-gray-500">
                This is exactly what your visitors see when they open your booking link.
              </p>
            </div>

            {/* Browser chrome mockup */}
            <div className="mt-12 overflow-hidden rounded-2xl border border-gray-200 shadow-2xl">
              {/* Browser bar */}
              <div className="flex items-center gap-2 border-b border-gray-200 bg-gray-100 px-4 py-3">
                <span className="h-3 w-3 rounded-full bg-red-400" />
                <span className="h-3 w-3 rounded-full bg-yellow-400" />
                <span className="h-3 w-3 rounded-full bg-green-400" />
                <div className="ml-4 flex-1 rounded-md bg-white px-3 py-1 text-xs text-gray-400">
                  openslot.app/book/intro-call
                </div>
              </div>

              {/* Mockup content */}
              <div className="bg-white p-10">
                <div className="mx-auto max-w-md">
                  <h3 className="text-xl font-bold text-gray-900">30-min Intro Call</h3>
                  <p className="mt-1 text-sm text-gray-500">Pick a time that works for you.</p>

                  <div className="mt-6 rounded-xl border border-gray-100 bg-gray-50 p-4 text-center text-sm text-gray-400">
                    ← June 2026 →
                  </div>

                  <div className="mt-4 grid grid-cols-3 gap-2">
                    {['9:00 AM', '9:30 AM', '10:00 AM', '10:30 AM', '11:00 AM', '2:00 PM'].map((t, i) => (
                      <button
                        key={t}
                        disabled
                        className={`rounded-lg border px-3 py-2 text-sm font-medium transition-colors ${
                          i === 2
                            ? 'border-indigo-600 bg-indigo-600 text-white'
                            : 'border-gray-200 bg-white text-gray-700'
                        }`}
                      >
                        {t}
                      </button>
                    ))}
                  </div>

                  <div className="mt-6 space-y-3 rounded-xl border border-indigo-100 bg-indigo-50 p-4">
                    <div>
                      <label className="block text-xs font-medium text-gray-600">Your Name</label>
                      <div className="mt-1 h-9 rounded-lg border border-gray-200 bg-white" />
                    </div>
                    <div>
                      <label className="block text-xs font-medium text-gray-600">Your Email</label>
                      <div className="mt-1 h-9 rounded-lg border border-gray-200 bg-white" />
                    </div>
                    <button disabled className="w-full rounded-lg bg-indigo-600 py-2 text-sm font-semibold text-white opacity-80">
                      Confirm Booking
                    </button>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </section>

        {/* CTA banner */}
        <section className="bg-indigo-600 py-20 text-center">
          <div className="mx-auto max-w-2xl px-6">
            <h2 className="text-3xl font-bold text-white sm:text-4xl">Ready to simplify your scheduling?</h2>
            <p className="mt-4 text-indigo-200">
              Join hundreds of people who've replaced back-and-forth emails with a single link.
            </p>
            <Link
              href="/login"
              className="mt-8 inline-block rounded-xl bg-white px-8 py-3.5 text-base font-semibold text-indigo-600 shadow-md hover:bg-indigo-50 transition-colors"
            >
              Create your free account
            </Link>
          </div>
        </section>
      </main>

      {/* Footer */}
      <footer className="border-t border-gray-100 bg-white py-10">
        <div className="mx-auto flex max-w-6xl flex-col items-center justify-between gap-4 px-6 text-sm text-gray-500 sm:flex-row">
          <div className="flex items-center gap-2 font-semibold text-gray-700">
            <div className="flex h-6 w-6 items-center justify-center rounded-md bg-indigo-600 text-white">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" className="h-3.5 w-3.5">
                <path d="M12 2a10 10 0 1 0 10 10A10 10 0 0 0 12 2zm0 18a8 8 0 1 1 8-8 8 8 0 0 1-8 8zm.5-13H11v6l5.25 3.15.75-1.23-4.5-2.67z" />
              </svg>
            </div>
            OpenSlot
          </div>
          <p>© {new Date().getFullYear()} OpenSlot. All rights reserved.</p>
          <div className="flex gap-6">
            <a href="#" className="hover:text-indigo-600 transition-colors">Privacy</a>
            <a href="#" className="hover:text-indigo-600 transition-colors">Terms</a>
            <a href="#" className="hover:text-indigo-600 transition-colors">Contact</a>
          </div>
        </div>
      </footer>

    </div>
  )
}

